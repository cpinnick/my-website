import { useState, useRef, useEffect } from "react"; /* ══════════════════════════════════════════════════════ SOUND ENGINE (Web Audio API) ══════════════════════════════════════════════════════ */ let _audioCtx = null; function getCtx() { if (!_audioCtx) { const AC = window.AudioContext || window.webkitAudioContext; if (AC) _audioCtx = new AC(); } if (_audioCtx && _audioCtx.state === "suspended") _audioCtx.resume(); return _audioCtx; } // Soft whoosh — card deal in Truth function playWhoosh() { const ctx = getCtx(); if (!ctx) return; const len = ctx.sampleRate * 0.18; const buf = ctx.createBuffer(1, len, ctx.sampleRate); const d = buf.getChannelData(0); for (let i = 0; i < len; i++) d[i] = (Math.random()*2-1) * Math.pow(1 - i/len, 1.8); const src = ctx.createBufferSource(); src.buffer = buf; const filt = ctx.createBiquadFilter(); filt.type = "bandpass"; filt.Q.value = 0.8; filt.frequency.setValueAtTime(1200, ctx.currentTime); filt.frequency.exponentialRampToValueAtTime(180, ctx.currentTime + 0.18); const gain = ctx.createGain(); gain.gain.setValueAtTime(0.35, ctx.currentTime); gain.gain.exponentialRampToValueAtTime(0.001, ctx.currentTime + 0.18); src.connect(filt); filt.connect(gain); gain.connect(ctx.destination); src.start(); } // Low thud — Obey card flip function playThud() { const ctx = getCtx(); if (!ctx) return; const osc = ctx.createOscillator(); const gain = ctx.createGain(); osc.type = "sine"; osc.frequency.setValueAtTime(140, ctx.currentTime); osc.frequency.exponentialRampToValueAtTime(38, ctx.currentTime + 0.12); gain.gain.setValueAtTime(0.55, ctx.currentTime); gain.gain.exponentialRampToValueAtTime(0.001, ctx.currentTime + 0.18); osc.connect(gain); gain.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 0.18); // add a subtle noise crack on top const len = ctx.sampleRate * 0.06; const buf = ctx.createBuffer(1, len, ctx.sampleRate); const data = buf.getChannelData(0); for (let i = 0; i < len; i++) data[i] = (Math.random()*2-1) * (1 - i/len); const noise = ctx.createBufferSource(); noise.buffer = buf; const ng = ctx.createGain(); ng.gain.value = 0.15; noise.connect(ng); ng.connect(ctx.destination); noise.start(); } // Ascending chime — all Obey cards revealed function playChime() { const ctx = getCtx(); if (!ctx) return; [523.25, 659.25, 783.99].forEach((freq, i) => { const osc = ctx.createOscillator(); const gain = ctx.createGain(); osc.type = "sine"; osc.frequency.value = freq; const t = ctx.currentTime + i * 0.14; gain.gain.setValueAtTime(0, t); gain.gain.linearRampToValueAtTime(0.22, t + 0.015); gain.gain.exponentialRampToValueAtTime(0.001, t + 1.4); osc.connect(gain); gain.connect(ctx.destination); osc.start(t); osc.stop(t + 1.4); }); } // Ambient drone — Landing page let _ambientOscs = null; function startAmbient() { const ctx = getCtx(); if (!ctx || _ambientOscs) return; _ambientOscs = [55, 82.41, 110.0].map(freq => { const osc = ctx.createOscillator(); const gain = ctx.createGain(); osc.type = "sine"; osc.frequency.value = freq + (Math.random() - 0.5) * 1.5; gain.gain.setValueAtTime(0, ctx.currentTime); gain.gain.linearRampToValueAtTime(0.038, ctx.currentTime + 2.5); osc.connect(gain); gain.connect(ctx.destination); osc.start(); return { osc, gain }; }); } function stopAmbient() { const ctx = getCtx(); if (!ctx || !_ambientOscs) return; _ambientOscs.forEach(({ osc, gain }) => { gain.gain.setValueAtTime(gain.gain.value, ctx.currentTime); gain.gain.linearRampToValueAtTime(0, ctx.currentTime + 1.2); osc.stop(ctx.currentTime + 1.2); }); _ambientOscs = null; }// ── Intro sound — plays on first user interaction ────────── const INTRO_SOUND = "data:audio/mp4;base64,AAAAHGZ0eXBNNEEgAAAAAE00QSBpc29tbXA0MgAAAAFtZGF0AA // Module-level audio element — never garbage collected let _introAudio = null; let _introPlayed = false; try { _introAudio = new Audio(INTRO_SOUND); _introAudio.preload = "auto"; _introAudio.volume = 0.9; } catch(e) {} function _playIntroNow() { if (_introPlayed || !_introAudio) return; _introPlayed = true; _introAudio.currentTime = 0; _introAudio.play().catch(() => {}); } // Attach at module load — fires on the very first tap/click (function _attachIntroListeners() { const handler = () => { _playIntroNow(); document.removeEventListener("touchstart", handler, true); document.removeEventListener("pointerdown", handler, true); document.removeEventListener("mousedown", handler, true); }; document.addEventListener("touchstart", handler, { capture: true, passive: true }); document.addEventListener("pointerdown", handler, { capture: true }); document.addEventListener("mousedown", handler, { capture: true }); })(); // Tick-tock clock sound let _tickCount = 0; function playTick() { const ctx = getCtx(); if (!ctx) return; _tickCount++; const osc = ctx.createOscillator(); const gain = ctx.createGain(); osc.type = "sine"; osc.frequency.value = _tickCount % 2 === 0 ? 1050 : 880; // tick / tock gain.gain.setValueAtTime(0.4, ctx.currentTime); gain.gain.exponentialRampToValueAtTime(0.001, ctx.currentTime + 0.07); osc.connect(gain); gain.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 0.07); } // Old-fashioned alarm bell function playAlarm() { const ctx = getCtx(); if (!ctx) return; for (let ring = 0; ring < 5; ring++) { const t = ctx.currentTime + ring * 0.45; const osc = ctx.createOscillator(); const gain = ctx.createGain(); osc.type = "square"; osc.frequency.value = 880; // Rapid tremolo = ringing bell const lfo = ctx.createOscillator(); const lfoG = ctx.createGain(); lfo.frequency.value = 28; lfoG.gain.value = 0.4; lfo.connect(lfoG); lfoG.connect(gain.gain); gain.gain.setValueAtTime(0.32, t); gain.gain.setValueAtTime(0.32, t + 0.36); gain.gain.exponentialRampToValueAtTime(0.001, t + 0.44); osc.connect(gain); gain.connect(ctx.destination); lfo.start(t); lfo.stop(t + 0.44); osc.start(t); osc.stop(t + 0.44); } } // Deep commanding sound for Obey button // Obey button sound const _OBEY_BTN_B64 = "AAAAHGZ0eXBNNEEgAAACAE00QSBpc29taXNvMgAAAAhmcmVlAABQ9W1kYXTeAgBMYXZjNj let _obeyBtnBuffer = null; let _obeyBtnDecoding = false; function _decodeObeyBtn(ctx, onReady) { if (_obeyBtnBuffer) { onReady(); return; } if (_obeyBtnDecoding) return; _obeyBtnDecoding = true; try { const binary = atob(_OBEY_BTN_B64); const bytes = new Uint8Array(binary.length); for (let i = 0; i < binary.length; i++) bytes[i] = binary.charCodeAt(i); ctx.decodeAudioData(bytes.buffer).then(buf => { _obeyBtnBuffer = buf; _obeyBtnDecoding = false; onReady(); }).catch(() => { _obeyBtnDecoding = false; }); } catch(e) { _obeyBtnDecoding = false; } } function playObeyBtn() { try { const ctx = getCtx(); if (!ctx) return; _decodeObeyBtn(ctx, () => { const src = ctx.createBufferSource(); src.buffer = _obeyBtnBuffer; const gain = ctx.createGain(); gain.gain.value = 0.9; src.connect(gain); gain.connect(ctx.destination); src.start(); }); } catch(e) {} } // Dice rattle — Web Audio API, no file needed // Dice sound — from dice.m4a const _DICE_B64 = "AAAAHGZ0eXBNNEEgAAAAAE00QSBpc29tbXA0MgAAAAFtZGF0AAAAAAAA5C8hIANAaBwhTuM4h+ let _diceBuffer = null; let _diceDecoding = false; function _decodeDice(ctx, onReady) { if (_diceBuffer) { onReady(); return; } if (_diceDecoding) return; _diceDecoding = true; try { const binary = atob(_DICE_B64); const bytes = new Uint8Array(binary.length); for (let i = 0; i < binary.length; i++) bytes[i] = binary.charCodeAt(i); ctx.decodeAudioData(bytes.buffer).then(buf => { _diceBuffer = buf; _diceDecoding = false; onReady(); }).catch(() => { _diceDecoding = false; }); } catch(e) { _diceDecoding = false; } } function playDiceSound() { try { const ctx = getCtx(); if (!ctx) return; _decodeDice(ctx, () => { const src = ctx.createBufferSource(); src.buffer = _diceBuffer; const gain = ctx.createGain(); gain.gain.value = 0.9; src.connect(gain); gain.connect(ctx.destination); src.start(); }); } catch(e) {} } const _CARD_FLIP_B64 = "AAAAHGZ0eXBNNEEgAAAAAE00QSBpc29tbXA0MgAAAAFtZGF0AAAAAAAAHREhAANAaBwhA let _cardFlipBuffer = null; let _cardFlipDecoding = false; function _decodeCardFlip(ctx, onReady) { if (_cardFlipBuffer) { onReady(); return; } if (_cardFlipDecoding) return; _cardFlipDecoding = true; try { const binary = atob(_CARD_FLIP_B64); const bytes = new Uint8Array(binary.length); for (let i = 0; i < binary.length; i++) bytes[i] = binary.charCodeAt(i); ctx.decodeAudioData(bytes.buffer).then(buf => { _cardFlipBuffer = buf; _cardFlipDecoding = false; onReady(); }).catch(() => { _cardFlipDecoding = false; }); } catch(e) { _cardFlipDecoding = false; } } function playCardFlip() { try { const ctx = getCtx(); if (!ctx) return; const play = () => { if (!_cardFlipBuffer) return; const src = ctx.createBufferSource(); src.buffer = _cardFlipBuffer; const gain = ctx.createGain(); gain.gain.value = 0.9; src.connect(gain); gain.connect(ctx.destination); src.start(); }; _decodeCardFlip(ctx, play); } catch(e) {} } // Spinning wheel sound — whoosh sweep + decelerating clicks // Wheel spin sound — decoded lazily from uploaded wheel.m4a const _WHEEL_B64 = "AAAAHGZ0eXBNNEEgAAAAAE00QSBpc29tbXA0MgAAAAFtZGF0AAAAAAADcBwhIANAaBwhTus1B let _wheelBuffer = null; let _wheelDecoding = false; function _decodeWheel(ctx, onReady) { if (_wheelBuffer) { onReady(); return; } if (_wheelDecoding) return; _wheelDecoding = true; const binary = atob(_WHEEL_B64); const bytes = new Uint8Array(binary.length); for (let i = 0; i < binary.length; i++) bytes[i] = binary.charCodeAt(i); ctx.decodeAudioData(bytes.buffer).then(b => { _wheelBuffer = b; _wheelDecoding = false; onR } function playWheelSound() { const ctx = getCtx(); if (!ctx) return; _decodeWheel(ctx, () => { const src = ctx.createBufferSource(); src.buffer = _wheelBuffer; const g = ctx.createGain(); g.gain.value = 0.9; src.connect(g); g.connect(ctx.destination); src.start(); }); } // Pen-on-paper scratch sound function playPenSound() { const ctx = getCtx(); if (!ctx) return; const len = Math.floor(ctx.sampleRate * 0.018); const buf = ctx.createBuffer(1, len, ctx.sampleRate); const d = buf.getChannelData(0); for (let i = 0; i < len; i++) d[i] = (Math.random()*2-1) * Math.pow(1-i/len, 0.8) * 0.12; const src2 = ctx.createBufferSource(); src2.buffer = buf; const f1 = ctx.createBiquadFilter(); f1.type="bandpass"; f1.frequency.value=3000+Math.rando const f2 = ctx.createBiquadFilter(); f2.type="highpass"; f2.frequency.value=2500; const g = ctx.createGain(); g.gain.value=0.9; src2.connect(f1); f1.connect(f2); f2.connect(g); g.connect(ctx.destination); src2.start(); src2.stop(ctx.currentTime+0.02); } /* ══════════════════════════════════════════════════════ FONTS ══════════════════════════════════════════════════════ */ const injectFont = () => { if (document.getElementById("desire-fonts")) return; const l = document.createElement("link"); l.id = "desire-fonts"; l.rel = "stylesheet"; l.href = "https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400 document.head.appendChild(l); }; const PHOTOS = [ `data:image/webp;base64,UklGRngLAABXRUJQVlA4IGwLAABwaACdASriACwBPulqrk4pJqmmL3K5qTAdCWlu4ES `data:image/webp;base64,UklGRlgbAABXRUJQVlA4IEwbAABwywCdASqkARkBPulqrVEpKzKupxIcKlAdCWVuQoA `data:image/webp;base64,UklGRlIZAABXRUJQVlA4IEYZAABwmgCdASqkARkBPulusFIpJjQzJLNq8oAdCWduzpR `data:image/webp;base64,UklGRnAKAABXRUJQVlA4IGQKAACQVQCdASrOACwBPuloqU6pKrEmL9O6CiAdCWdu3V2 `data:image/webp;base64,UklGRrIHAABXRUJQVlA4IKYHAABwSwCdASrIACwBPuluqU8pNyymrTOp0uAdCWluzoA `data:image/webp;base64,UklGRgwUAABXRUJQVlA4IAAUAAAwbQCdASrhACwBPulqrlIpJaQro7EMsXAdCWluAzo `data:image/webp;base64,UklGRk4IAABXRUJQVlA4IEIIAADwWACdASrIACwBPulmqFApOrUxLZIL01AdCWluJjC `data:image/webp;base64,UklGRpAgAABXRUJQVlA4IIQgAABwvwCdASqkARkBPulorVMpJTK4I3GcWwAdCWluVDM `data:image/webp;base64,UklGRlYOAABXRUJQVlA4IEoOAACwYACdASrwACwBPulsr1KpJbOno1Nb6nAdCWkn3qg `data:image/webp;base64,UklGRooSAABXRUJQVlA4IH4SAADQlwCdASo8ASwBPulmrVCpJS4xqNJMcjAdCWlttoD `data:image/webp;base64,UklGRjAWAABXRUJQVlA4ICQWAABwhQCdASrgACwBPulmrE8pJakiKNNNCSAdCWkA2Mu `data:image/webp;base64,UklGRuQLAABXRUJQVlA4INgLAABwRgCdASrIACwBPulorlQpJSmsodErWZAdCWlu0aV `data:image/webp;base64,UklGRhoTAABXRUJQVlA4IA4TAABQnwCdASqkARgBPulqr1ApJji0qDI7oxAdCWktjMm `data:image/webp;base64,UklGRqYNAABXRUJQVlA4IJoNAABQcgCdASrIACwBPulirE4pJbKsLFGNIlAdCWkHClo `data:image/webp;base64,UklGRlIKAABXRUJQVlA4IEYKAAAQZACdASqkARcBPulyr1GpLCmnqRO42YAdCWdu3/q `data:image/webp;base64,UklGRpAGAABXRUJQVlA4IIQGAAAQPQCdASrIACwBPulwsVOpJi80olD6SpAdCWluvUh `data:image/webp;base64,UklGRmIWAABXRUJQVlA4IFYWAABwdACdASrIACwBPulmrFEpJS2updEd4dAdCWkvvhg `data:image/webp;base64,UklGRiomAABXRUJQVlA4IB4mAACw3ACdASqkARgBPulmrFCpJSkxp5BNijAdCWVt/K7 `data:image/webp;base64,UklGRiIOAABXRUJQVlA4IBYOAABwZACdASrIACwBPulkqk4pJaQmLzObUSAdCWkA2Eb `data:image/webp;base64,UklGRm4NAABXRUJQVlA4IGINAACwewCdASqkARkBPulsrVE/sLcqqbUqC/AdCWcttUi `data:image/webp;base64,UklGRgoMAABXRUJQVlA4IP4LAAAQRACdASrIACwBPulkrlQpJK4xItDLMiAdCWkzun7 `data:image/webp;base64,UklGRvQHAABXRUJQVlA4IOgHAABQTACdASr6ACwBPulwslKpJiorpdIKCXAdCWluu79 `data:image/webp;base64,UklGRuAIAABXRUJQVlA4INQIAABQXgCdASoeASwBPuluq0+pPTaqKxYaa6AdCWduvUy `data:image/webp;base64,UklGRtgcAABXRUJQVlA4IMwcAACw1QCdASqkARgBPulorU+pJjw7KPM8U4AdCWVuQpY `data:image/webp;base64,UklGRlwXAABXRUJQVlA4IFAXAAAwpwCdASomASwBPulkrE8pJa2mqdENSbAdCWltVvM `data:image/webp;base64,UklGRiIRAABXRUJQVlA4IBYRAAAwcgCdASrgACwBPulorU4pKrKsLxRaQlAdCWlu4Pp `data:image/webp;base64,UklGRh4NAABXRUJQVlA4IBINAAAQXQCdASqoACwBPulorE6pJikjLHHL8SAdCWcAz9F `data:image/webp;base64,UklGRhwQAABXRUJQVlA4IBAQAAAQkwCdASo7ASwBPuFgo00oryeitHOMUeAcCWltEOU `data:image/webp;base64,UklGRrQNAABXRUJQVlA4IKgNAAAQWwCdASrIACwBPulkrVMpJKOoI5CciSAdCWkIcAG `data:image/webp;base64,UklGRlAHAABXRUJQVlA4IEQHAADQQQCdASrIACwBPulssFOpJT+woxC7C/AdCWlulZA `data:image/webp;base64,UklGRtgJAABXRUJQVlA4IMwJAACwXQCdASoxASwBPuluq1ApNzaqK9KpyuAdCWluvVB `data:image/webp;base64,UklGRkwJAABXRUJQVlA4IEAJAADwVQCdASrIACwBPulssFEpJjQ1pvIrKrAdCWktlJn `data:image/webp;base64,UklGRh4NAABXRUJQVlA4IBINAADQXgCdASrIACwBPulkp02pKjCmMBTrQhAdCWUtgFg `data:image/webp;base64,UklGRogKAABXRUJQVlA4IHwKAABwQgCdASrmACwBPulyrlIpJy4vp3LpwfAdCWMHLEK `data:image/webp;base64,UklGRvIGAABXRUJQVlA4IOYGAACwOwCdASreACwBPulurlEpJrMqqPO58mAdCWduvQU ]; /* ══════════════════════════════════════════════════════ CATEGORIES ══════════════════════════════════════════════════════ */ const CATS = [ { id:"fantasy", label:"FANTASY", icon:"✦", color:"#C9A84C", bg:["#2a0a14","#1a0510","#0e0408"], glow:["rgba(200,50,80,0.55)","rgba(120,10,40,0.45)","rgba(80,0,30,0.3)"] }, { id:"adventurous", label:"ADVENTUROUS", icon:" ", color:"#C9A84C", bg:["#0e1228","#0a0d1e","#060810"], glow:["rgba(60,20,160,0.5)","rgba(140,30,60,0.4)","rgba(40,0,80,0.3)"] }, { id:"dominant", label:"DOMINANT", icon:"◆", color:"#b47afa", bg:["#100820","#0c0518","#070310"], glow:["rgba(120,30,200,0.5)","rgba(160,20,100,0.4)","rgba(60,0,100,0.3)"] }, { id:"submissive", label:"SUBMISSIVE", icon:"◇", color:"#7ab4fa", bg:["#081028","#05091e","#030510"], glow:["rgba(20,60,180,0.5)","rgba(60,20,140,0.4)","rgba(10,0,80,0.3)"] }, ]; /* ══════════════════════════════════════════════════════ OFFLINE QUESTION BANK (guarantees infinite cards) ══════════════════════════════════════════════════════ */ const BANK = { fantasy: [ { q:"Describe a scene you've imagined but never dared share — the one that makes you paus { q:"What's one fantasy that feels too dark to say out loud, but you're saying it anyway? { q:'Complete this sentence: "The filthiest instruction I\'d want whispered in my ear is… { q:"If we had 24 hours with no consequences and no rules, where does your mind go first? { q:"Tell me a fantasy you've replayed so many times you practically have a script for it { q:"What scenario have you imagined involving a stranger that you'd never admit to in da { q:"If you could watch me do one thing without me knowing, what would it be?", i:3 }, { q:'Complete this sentence: "The scene I\'ve never told anyone about starts with…"', i:4 { q:"What's the most forbidden setting you've ever imagined us in?", i:3 }, { q:"Describe exactly what you'd want if I told you tonight you could ask for anything.", { q:"What's something you've watched or read that gave you a fantasy you've never confess { q:"If your desires had no filter right now, what's the first image that comes to mind?" ], adventurous: [ { q:'Complete this sentence: "If you tied my hands up right now, I\'d want you to…"', i:4 { q:'Complete this sentence: "If you told me not to move, I would…"', i:4 }, { q:"What would you do to me if I handed you a blindfold and said I trust you completely? { q:"Describe exactly how you'd use restraints on me if you had total permission.", i:4 } { q:"If you could pick one act we've never done that involves giving up control, what wou { q:'Complete this sentence: "I want you to grab me by the ___ and tell me ___."', i:4 }, { q:"What's one location that makes you think about doing something we absolutely shouldn { q:"If I dared you to push one limit tonight, which one would you choose?", i:4 }, { q:"Tell me what you'd do if you had me bound and completely at your mercy for one hour. { q:"What's the most daring thing you've thought about doing to me in public?", i:3 }, { q:"If we tried edging tonight, would you want to be the one in control or the one unrav { q:"Describe a scene involving sensory deprivation — what would you take away from me fi ], dominant: [ { q:"Give me one explicit command — something you'd make me do without asking twice.", i: { q:"Tell me one rule you'd set for tonight if you were in full control of everything.", { q:"What's something you'd make me say, out loud, that you've never made me say before?" { q:"If you were directing tonight's scene entirely, what's the first instruction you'd g { q:"Describe exactly how you'd want me positioned right now if you were fully in charge. { q:"What's one way you'd assert control tonight that you've held back from doing?", i:3 { q:"If you could choose my outfit, what I can and can't touch, and where I have to be — { q:"Tell me what you'd make me do if I said the words 'I'm yours tonight, completely.'", { q:"What's one thing you've wanted to demand from me but talked yourself out of?", i:3 } { q:"If you could control exactly how close to the edge I get tonight — how would you pla { q:"Describe one way you'd use eye contact or silence to assert control over me tonight. { q:"What's the one command you'd give me that you know would make me completely submissi ], submissive: [ { q:"Tell me one thing you secretly want to be told to do, that you've never admitted wan { q:"What's one instruction you're hoping I give you tonight that you'd follow without qu { q:"Describe the feeling you want — the moment you completely let go and just obey.", i: { q:"What's one form of surrender you've imagined but never asked for?", i:3 }, { q:"If I told you to hold still no matter what I did, what's the face you'd be making?", { q:"Tell me what it would feel like to hear me say 'you don't get to decide anything ton { q:"What's one boundary you'd want me to push right up against — and then stop just befo { q:"Describe what it feels like in your body when you know I'm fully in control.", i:3 } { q:"What's a word or phrase from me that would instantly put you in that headspace?", i: { q:"Tell me one act of service or submission you've fantasized about performing.", i:4 } { q:"If I stripped away every decision from you for the next hour, what emotion would tha { q:"What does it feel like when you realize you want someone to just take over completel ], }; /* ══════════════════════════════════════════════════════ HELPERS ══════════════════════════════════════════════════════ */ function shuffle(arr) { const a = [...arr]; for (let i = a.length - 1; i > 0; i--) { const j = Math.floor(Math.random() * (i + 1)); [a[i], a[j]] = [a[j], a[i]]; } return a; } function randomCat(filter) { if (filter !== "all") return filter; const ids = Object.keys(BANK); return ids[Math.floor(Math.random() * ids.length)]; } function getOfflineCard(catId, usedSet) { const pool = BANK[catId] || BANK.fantasy; // prefer unused const unused = pool.filter((_, idx) => !usedSet.has(`${catId}-${idx}`)); const src = unused.length > 0 ? unused : pool; const idx = Math.floor(Math.random() * src.length); const entry = src[idx]; const realIdx = unused.length > 0 ? pool.indexOf(entry) : idx; usedSet.add(`${catId}-${realIdx}`); return { q: entry.q, i: entry.i }; } /* ══════════════════════════════════════════════════════ CARD VISUAL (pure CSS — works in any sandbox) ══════════════════════════════════════════════════════ */ function CardArt({ cat, seed, photoSrc }) { const [loaded, setLoaded] = useState(false); if (photoSrc) { return (
setLoaded(true)} style={{ width:"100%", height:"auto", maxHeight:"220px", display:"block", objectFit filter:"brightness(0.92) contrast(1.0) saturate(0.85)", opacity: loaded?1:0, transition:"opacity 0.5s ease" }} />
); } // fallback CSS art const id = `art-${seed}`; // Pseudo-random positions from seed const rng = (n) => ((seed * 9301 + n * 49297) % 233280) / 233280; const blobs = [ { cx: `${20 + rng(1) * 60}%`, cy: `${15 + rng(2) * 40}%`, rx: 110 + rng(3) * 80, ry: 80 + { cx: `${20 + rng(5) * 60}%`, cy: `${40 + rng(6) * 45}%`, rx: 90 + rng(7) * 70, ry: 70 + { cx: `${10 + rng(9) * 80}%`, cy: `${20 + rng(10) * 60}%`, rx: 60 + rng(11) * 50, ry: 50 ]; const lineY = [rng(13) * 260, 80 + rng(14) * 160, 160 + rng(15) * 100]; return (
{/* Atmospheric color blobs */} {blobs.map((b,i) => ( ))} {/* Subtle diagonal lines */} {lineY.map((y,i) => ( ))} {/* Center hot-spot */} {/* Vignette */} {/* Grain texture overlay */}
{/* Bottom fade */}
); } /* ══════════════════════════════════════════════════════ SINGLE CARD ══════════════════════════════════════════════════════ */ function Card({ card, phase }) { const cat = CATS.find(c=>c.id===card.cat) || CATS[0]; return (
{/* Gold top accent */}
{/* Category label */}
{/* Question */}
{card.q}

); } /* ══════════════════════════════════════════════════════ MAIN APP ══════════════════════════════════════════════════════ */ function DesireApp({ onBack }) { injectFont(); // ── All mutable state lives in a single ref (zero stale-closure risk) ── const R = useRef({ deck: [], // array of card objects idx: -1, // current index favorites: new Set(), used: new Set(), generating:false, seedCtr: 0, }); // React render trigger only const [, redraw] = useState(0); const bump = () => redraw(n => n+1); const [catFilter, setCatFilter] = useState("all"); const [phase, setPhase] const [status, setStatus] = useState(""); = useState("in"); // "idle"|"out"|"in" // ── Build a card object ──────────────────────────────────── const makeCard = (catId, question, intensity) => { const s = R.current.seedCtr++; const photo = PHOTOS[Math.floor(Math.random() * PHOTOS.length)]; return { id:Date.now()+Math.random(), cat:catId, q:question, intensity, seed:s, photo }; }; // ── Try API, fall back to offline bank instantly ─────────── const fetchCard = async (catId) => { const prompts = { fantasy: "sexual fantasy exploration for consenting adults — dark desires, forbidde adventurous: "BDSM and kink between consenting adults — bondage, restraints, power exch dominant: "dominant/top role — giving explicit commands, taking full control, dire submissive: "submissive/bottom role — surrendering control, following orders, being ta }; try { const res = await fetch("https://api.anthropic.com/v1/messages", { method:"POST", headers:{"Content-Type":"application/json"}, body:JSON.stringify({ model:"claude-sonnet-4-20250514", max_tokens:300, messages:[{role:"user",content: `Write one adult couples card question about ${prompts[catId]}. Bold, explicit, and direct. Max 2 sentences. Style: sentence-completion or provocative Reply ONLY with JSON (no markdown): {"question":"...","intensity":1-4}` }], questi }), }); if (!res.ok) throw new Error(`${res.status}`); const data = await res.json(); const txt = (data.content||[]).map(b=>b.text||"").join("").replace(/```[\w]*/g,"").trim const p = JSON.parse(txt); if (p.question) return { q:p.question, i:Math.min(4,Math.max(1,Number(p.intensity)||3)) } catch(e) { console.warn("API failed, using offline bank:", e.message); } // Offline fallback — always works return getOfflineCard(catId, R.current.used); }; // ── Generate next card and advance ──────────────────────── const advance = async () => { const r = R.current; if (r.generating) return; const nextIdx = r.idx + 1; // Shuffle out setPhase("out"); if (nextIdx < r.deck.length) { setTimeout(() => { r.idx = nextIdx; bump(); setPhase("in"); }, 260); return; } // Need a new card — generate while card is flying out r.generating = true; setStatus("loading"); const catId = randomCat(catFilter); const { q, i } = await fetchCard(catId); const card = makeCard(catId, q, i); r.deck.push(card); r.idx = nextIdx; r.generating = false; setStatus(""); bump(); setPhase("in"); }; // ── Regenerate current card in-place ────────────────────── const regen = async () => { const r = R.current; if (r.generating || r.idx < 0) return; r.generating = true; setStatus("loading"); setPhase("out"); const catId = randomCat(catFilter); const { q, i } = await fetchCard(catId); const card = makeCard(catId, q, i); r.deck[r.idx] = card; r.generating = false; setStatus(""); bump(); setPhase("in"); }; const toggleFav = () => { const r = R.current; const card = r.deck[r.idx]; if (!card) return; if (r.favorites.has(card.id)) r.favorites.delete(card.id); else r.favorites.add(card.id); bump(); }; // ── Seed first card on mount ─────────────────────────────── useEffect(() => { advance(); }, []); // eslint-disable-line const r = R.current; const card = r.deck[r.idx]; const isFav = card ? r.favorites.has(card.id) : false; const loading = status === "loading"; /* ── Styles ─────────────────────────────────────────────── */ return (
{/* Ambient bg glow */} {[{t:"-20%",l:"-10%",c:"rgba(100,0,35,0.07)"},{b:"-20%",r:"-10%",c:"rgba(40,0,70,0.07)"
))} {/* ── Header ─────────────────────────────────────────── */}
← back
DESIRE
{r.favorites.size>0&&(
♥ {r.favorites.size} saved
)}
{/* ── Category pills ─────────────────────────────────── */}
{[{id:"all",label:"All"},...CATS].map(c=>( setCatFilter(c.id)} style={{ whiteSpace:"nowrap",padding:"6px 16px",borderRadius:"20px",flexShrink:0, background: catFilter===c.id ? "rgba(192,57,43,0.3)" : "rgba(192,57,43,0.1)", border:`1px solid rgba(192,57,43,${catFilter===c.id?"0.6":"0.35"})`, color:"#f0e4d4", fontSize:"15px",fontStyle:"italic",fontFamily:"'Cormorant Garamond',cursive", fontWeight:"500",letterSpacing:"0.5px", boxShadow: catFilter===c.id?"0 2px 12px rgba(192,57,43,0.4)":"none", transition:"all 0.2s", }}> {c.label.charAt(0)+c.label.slice(1).toLowerCase()} ))}
{/* ── Card area ──────────────────────────────────────── */}
{card ? : (
{loading ? "Generating your first card…" : ""}
) }
{/* ── Controls ───────────────────────────────────────── */}
{/* Regenerate */} ↺ {/* Next Card */} { if (navigator.vibrate) navigator.vibrate(30); playWhoosh(); style={{ flex:1,height:"52px",borderRadius:"30px", background: loading ? "rgba(192,57,43,0.05)" : "rgba(192,57,43,0.15)", border:"1px solid rgba(192,57,43,0.5)", color:"#f0e4d4",fontSize:"22px", fontFamily:"'Cormorant Garamond',cursive",fontWeight:"400",fontStyle:"italic", letterSpacing:"1px", display:"flex",alignItems:"center",justifyContent:"center", boxShadow: loading?"none":"0 4px 24px rgba(192,57,43,0.45)", animation: loading?"none":"pulse 2.5s ease infinite", transition:"background 0.3s", }}> {loading ? "generating…" : "Next"} {/* Favorite */} {isFav?"♥":"♡"}
); } /* ══════════════════════════════════════════════════════ OBEY GAME DATA ══════════════════════════════════════════════════════ */ // Types: L=lying, K=kneeling, S=standing, C=seated const OBEY_POS = [ { text:"Lay flat on your back", t:"L" }, { text:"Lay flat on your stomach", t:"L" }, { text:"Get down on your knees", t:"K" }, { text:"Bend over a couch or chair", t:"S" }, { text:"Stand against the wall", t:"S" }, { text:"Sit with your legs spread apart", t:"C" }, { text:"Kneel at the edge of the bed", t:"K" }, { text:"Arch your back slowly", t:"S" }, { text:"Sit on the floor with your hands behind you", t:"C" }, { text:"Cross your wrists behind your back", t:"S" }, { text:"Sit on your partner\u2019s lap facing away", t:"C" }, { text:"Stand with your eyes closed", t:"S" }, { text:"Lie sideways with one leg raised", t:"L" }, { text:"Straddle a pillow or cushion", t:"C" }, { text:"Pose like you\u2019re being watched", t:"LKSC" }, { text:"Crawl slowly across the room", t:"K" }, { text:"Sit in a chair and lean back confidently", t:"C" }, { text:"Press your body against a mirror", t:"S" }, { text:"Stand on your tiptoes against the wall", t:"S" }, { text:"Kneel with your hands on your thighs", t:"K" }, { text:"Sit with your ankles crossed", t:"C" }, { text:"Lie down and stretch dramatically", t:"L" }, { text:"Lean forward with your hands on your knees", t:"S" }, { text:"Sit at the edge of the bed and tease slowly", t:"C" }, { text:"Face away from your partner and look back", t:"S" }, { text:"Stand in a doorway and hold the frame", t:"S" }, { text:"Sit with your hands restrained behind your back", t:"C" }, { text:"Kneel between your partner\u2019s legs", t:"K" }, { text:"Slowly turn in a circle like a tease performance", t:"S" }, { text:"Freeze in your most seductive pose", t:"LKSC" }, ]; const OBEY_ACT = [ { text:"Slowly run your hands over your body", c:"LKSC" }, { text:"Massage your chest sensually", c:"LKSC" }, { text:"Whisper something naughty", c:"LKSC" }, { text:"Tease yourself using only your fingertips", c:"LKSC" }, { text:"Give a slow lap dance", c:"SC" }, { text:"Kiss your partner everywhere except the lips", c:"LKSC" }, { text:"Maintain intense eye contact", c:"KSC" }, { text:"Trace circles along your thighs", c:"LKSC" }, { text:"Lightly tug your hair", c:"KSC" }, { text:"Slowly remove one item of clothing", c:"KSC" }, { text:"Bite your lip while teasing", c:"LKSC" }, { text:"Tease your partner slowly with an ice cube", c:"LKSC" }, { text:"Blindfold your partner and tease them", c:"LKSC" }, { text:"Drag your nails slowly across your partner\u2019s skin", c:"LKSC" }, { text:"Grind slowly to music", c:"SC" }, { text:"Tease without actually touching skin", c:"LKSC" }, { text:"Sit in your partner\u2019s lap and move slowly", c:"SC" }, { text:"Slowly unbutton your shirt while staring", c:"KSC" }, { text:"Caress your body while your partner watches", c:"LKSC" }, { text:"Whisper what you want done to you", c:"LKSC" }, { text:"Tease your partner with feather-light touches", c:"LKSC" }, { text:"Slowly kiss from shoulder to stomach", c:"LKSC" }, { text:"Use only one hand to tease", c:"LKSC" }, { text:"Move in slow motion the entire time", c:"LKSC" }, { text:"Run your hands slowly down your partner\u2019s body", c:"LKSC" }, { text:"Dance slowly without breaking eye contact", c:"S" }, { text:"Press your body close without kissing", c:"KS" }, { text:"Slowly circle your hips while being watched", c:"SC" }, // ── New actions ── { text:"Follow every instruction without speaking unless spoken to", c:"LKSC" }, { text:"Hold eye contact and don\u2019t look away until commanded", c:"KSC" }, { text:"Trace your lips slowly with your fingertip", c:"LKSC" }, { text:"Run your tongue slowly across your bottom lip", c:"LKSC" }, { text:"Keep your hands completely still no matter what happens", c:"KSC" }, { text:"Describe out loud exactly what you want done to you", c:"LKSC" }, { text:"Slowly pull your hair to one side and expose your neck", c:"KSC" }, { text:"Grip whatever is nearest and don\u2019t let go", c:"LKSC" }, { text:"Press your palms flat against your thighs and hold them there", c:"KC" }, { text:"Reach behind you and lace your fingers together", c:"LKSC" }, { text:"Slowly drag your bottom lip between your teeth and hold it", c:"LKSC" }, { text:"Whisper your partner\u2019s name slowly without being asked", c:"LKSC" }, { text:"Run a single fingertip slowly down the center of your chest", c:"KSC" }, { text:"Keep your chin raised and your gaze steady", c:"KSC" }, { text:"Press your wrists together and extend them toward your partner", c:"LKSC" }, { text:"Take a slow breath and don\u2019t exhale until told to", c:"LKSC" }, { text:"Slowly roll your entire body in one long deliberate movement", c:"SC" }, { text:"Keep your back arched and hold the position", c:"LSC" }, { text:"Run both hands slowly from your neck all the way to your hips", c:"KSC" }, { text:"Stay completely still and let your partner take you in", c:"LKSC" }, { text:"Count down from ten out loud, slowly and deliberately", c:"LKSC" }, { text:"Softly moan once, then go completely silent", c:"LKSC" }, { text:"Cross your wrists behind your back and keep them there", c:"KSC" }, { text:"Slowly close your eyes and don\u2019t open them until told", c:"LKSC" }, { text:"Tell your partner one thing you\u2019ve never admitted wanting", c:"LKSC" }, { text:"Move only in slow motion for the entire time", c:"KSC" }, { text:"Tease your partner\u2019s inner thigh with only your fingertips", c:"SC" }, { text:"Place your partner\u2019s hand exactly where you want it", c:"LKSC" }, { text:"Slowly circle your hips once, then freeze completely", c:"SC" }, { text:"Keep eye contact and slowly remove one thing you\u2019re wearing", c:"KSC" }, { text:"Touch only with your lips \u2014 no hands allowed", c:"LKSC" }, ]; // ── Shuffle bag system — exhausts full deck before repeating ── let _posBag = []; // positions deck let _usedAct = new Set(); // tracks used actions until all compatible ones exhausted function _shuffleArray(arr) { const a = [...arr]; for (let i = a.length - 1; i > 0; i--) { const j = Math.floor(Math.random() * (i + 1)); [a[i], a[j]] = [a[j], a[i]]; } return a; } function pickObeyRound() { // Positions: deal from shuffled deck — see all 30 before any repeats if (_posBag.length === 0) _posBag = _shuffleArray(OBEY_POS); const pos = _posBag.pop(); // Actions: filter compatible ones, avoid recently used until pool exhausted const compatible = OBEY_ACT.filter(a => pos.t === "LKSC" ? true : pos.t.split("").some(ch => a.c.includes(ch)) ); let unused = compatible.filter(a => !_usedAct.has(a.text)); if (unused.length === 0) { _usedAct.clear(); unused = compatible; } const act = unused[Math.floor(Math.random() * unused.length)]; _usedAct.add(act.text); const time = (Math.floor(Math.random() * 4) + 1) * 5; return { pos, act, time }; } /* ══════════════════════════════════════════════════════ FLIP CARD ══════════════════════════════════════════════════════ */ function FlipCard({ label, content, canFlip, flipped, onFlip }) { return (
{ if (canFlip && !flipped) { playCardFlip(); onFlip(); } }} style={{ perspective:"800px", width:"110px", flexShrink:0, cursor: canFlip&&!flipped?"p
{/* Back face */}
{/* Front face */}
{/* Tap hint */} {canFlip && !flipped && (
)}
); } /* ══════════════════════════════════════════════════════ DICE GAME ══════════════════════════════════════════════════════ */ const DICE_POSITIONS = [ "Sub lies down with ass up", "Sub sits on knees before Dom", "Sub kneels with legs open", "Sub lies over Dom\u2019s knee", "Sub straddles Dom", "Sub sits in Dom\u2019s lap, legs wide open", ]; const SHOWOFF_POSITIONS = [ "Do a strip tease for Dom", "Bend over and show Dom your holes", "Dom caresses and inspects sub for 2 mins", "Dom takes a picture of their choice", "Sub gives Dom a lap dance", "Dom\u2019s Choice", ]; const SUBS_SECRET_POSITIONS = [ "Tell Dom your dream roleplay", "Describe the last time you masturbated to Dom", "Tell Dom about your favourite porn", "Tell Dom your biggest sub fantasy", "Tell Dom a sexual act you would like to try", "Tell Dom the sluttiest thing you\u2019ve ever done", ]; const SADISTIC_POSITIONS = [ "Bend over furniture and let Dom use the paddle on sub\u2019s bare ass", "Bend over Dom\u2019s knees and have Dom spank sub with bare hand", "Dom restrains sub\u2019s hands and flogs sub\u2019s tits", "Dom edges sub for 5 mins with no release", "Dom makes sub cum for 5 mins", "Dom throat fucks sub until her eyes water", ]; // Which container rotation shows each face (0-indexed) const FACE_ROT = [ { x:0, y:0 }, // 1 front { x:0, y:-90 }, // 2 right { x:-90, y:0 }, // 3 top { x:90, y:0 }, // 4 bottom { x:0, y:90 }, // 5 left { x:0, y:180 }, // 6 back ]; // Dot layouts for each face value 1-6 const DOT_LAYOUT = [ [[50,50]], [[28,28],[72,72]], [[28,28],[50,50],[72,72]], [[28,28],[72,28],[28,72],[72,72]], [[28,28],[72,28],[50,50],[28,72],[72,72]], [[28,22],[72,22],[28,50],[72,50],[28,78],[72,78]], ]; function DieFace({ faceNum, transform }) { return (
{DOT_LAYOUT[faceNum].map(([x,y],i) => (
))}
); } // Reusable single-dice section function DiceSection({ label, positions, accentColor }) { const [phase, setPhase] = useState("ready"); const [result, setResult] = useState(null); const [rot, setRot] = useState({ x:-15, y:20 }); const timerRef = useRef(null); const roll = () => { if (phase === "rolling") return; clearTimeout(timerRef.current); playDiceSound(); const face = Math.floor(Math.random() * 6); const spins = 2 + Math.floor(Math.random() * 2); setPhase("rolling"); setResult(null); setRot({ x: FACE_ROT[face].x + spins*360, y: FACE_ROT[face].y + spins*360 }); timerRef.current = setTimeout(() => { setPhase("done"); setResult(face); }, 1800); }; const reset = () => { clearTimeout(timerRef.current); setPhase("ready"); setResult(null); setRot({ x:-15, y:20 }); }; useEffect(() => () => clearTimeout(timerRef.current), []); return (
{/* Label */}
color: {/* 3D Die */}
{/* Button / result */} {phase === "ready" && ( {label} )} {phase === "rolling" && (

Roll Again reset
)}
); } function DiceGame({ onBack }) { return (
